/**
 * Persona-AI has moved to a full-stack layout.
 *
 * Backend:  backend/src/index.js  (Express + OpenAI)
 * Frontend: frontend/             (React + Vite)
 *
 * Run from project root:
 *   npm install
 *   cp .env.example .env   # add OPENAI_API_KEY
 *   npm run dev
 */
console.log('Use "npm run dev" from the project root to start Persona-AI.');
